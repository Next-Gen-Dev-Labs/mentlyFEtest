import Animate from "@/components/common/animate";
import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Forms",
};

export default function Forms() {
  return <Animate>Forms</Animate>;
}
