import Animate from "@/components/common/animate";
import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Activities",
};

export default function Activities() {
  return <Animate>Activities</Animate>;
}
