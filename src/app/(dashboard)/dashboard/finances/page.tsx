import Animate from "@/components/common/animate";
import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Finances",
};

export default function Finances() {
  return <Animate>Finances</Animate>;
}
