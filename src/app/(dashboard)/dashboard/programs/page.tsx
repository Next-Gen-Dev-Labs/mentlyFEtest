import Animate from "@/components/common/animate";
import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Programs",
};

export default function Programs() {
  return <Animate>Programs</Animate>;
}
