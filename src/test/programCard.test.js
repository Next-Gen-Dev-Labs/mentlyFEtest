const assert = require('assert');
test('Program Card component renders correctly', () => {
	assert.strictEqual(true, true);
});
test('Program Card component handles interactions', () => {
	assert.strictEqual(true, true);
});